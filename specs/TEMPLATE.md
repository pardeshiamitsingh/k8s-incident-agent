# Spec: <feature name>

**Status:** draft | approved | implemented
**Constitution version this spec complies with:** 1.0.0

## Problem

What incident-agent capability does this spec add, and why does it matter
for the mission in `memory/constitution.md`?

## Scope

What's in. What's explicitly out (push to a later spec instead of scope-creeping).

## Constitution check

Walk each Core Principle from the constitution and state how this spec
complies. If it can't comply, stop and propose a constitution amendment
first instead of writing around it.

- Propose-only by default: 
- Read-only RBAC: 
- Local-first, no external LLM calls: 
- Every diagnosis is explainable: 
- Full audit trail: 

## Design

Interfaces, data shapes (e.g. LangGraph state fields touched/added), and
how this fits the existing graph/collectors/RAG store.

## Acceptance criteria

Concrete, testable conditions for "done" — ideally tied to the golden
incident set in `evals/`.

## Open questions

Anything still undecided that needs a human answer before implementation.
